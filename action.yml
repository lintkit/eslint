# action.yml
name: "LintKit: ESLint"
description: "An opinionated configuration to lint JavaScript and TypesScript files"
branding:
  icon: 'check'
  color: 'blue'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/lintkit/eslint:latest'
